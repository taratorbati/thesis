# src.controllers package — controller implementations for the irrigation thesis.
#
# All controllers implement the abstract base class in `base.py`, exposing
# a uniform interface to the runner (src.runner.run_season).
